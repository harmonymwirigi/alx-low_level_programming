#include <stdio.h>
#include <stdlib.h>
/**
 * main - the main function to return its name
 * @argc: argument count
 * @argv: argument count
 * Return: nothing
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s\n", argv[0])
	}
	return (0);
}
